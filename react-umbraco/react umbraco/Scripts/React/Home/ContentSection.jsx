import React from "react";

const ContentSection = ({ fullwidth}) => {
 
    return (
        
        <section className="section section">
            {fullwidth.fullWidthContent}
        </section>   
        
        
     )

}

export default ContentSection;